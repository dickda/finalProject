<p id="notice"><%= notice %></p>

    <div class="media-left media-middle"><%= image_tag @product.screenshots.first.image_url(:thumb) if @product.screenshots.first %> </div>
    <div class = " media-body">
    <div><h3> <%= link_to @product.title , @product %></h3></div>
    <div>Price: $ <%= @product.price %></div>
    <div class = "description"><p> <%= @product.description %></p></div>
   </div>
    <div>Stock: <%= @product.stock %></div>
    <div>Actions: <%= link_to 'Edit', edit_product_path(@product) %> 
    <%= link_to 'Destroy', @product, method: :delete, data: { confirm: 'Are you sure?' } %></div>
    <div class="actions">
      <%= link_to "Add to Cart", line_items_path(:product_id => @product, :quantity => 1), :method => :post %>  

    </div>